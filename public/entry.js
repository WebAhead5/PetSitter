function searchForm(){
    animate();
    setTimeout(() => location.href="search.html",1500);
}

function registryForm(){
    animate();
    setTimeout(() => location.href="registration.html",1500);
}